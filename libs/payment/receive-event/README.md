# receive-event-client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build receive-event-client` to build the library.

## Running unit tests

Run `nx test receive-event-client` to execute the unit tests via [Jest](https://jestjs.io).
